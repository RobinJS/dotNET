using System;
using System.Threading;
using System.Globalization;
using System.Numerics;

class Program
{
    static void Main()
    {
        //Thread.CurrentThread.CurrentCulture = CultureInfo.InvariantCulture;
        //string entry = Console.ReadLine();
        //BigInteger n;
        //if (entry.IndexOf('.') != -1 || entry.IndexOf('-') != -1)
        //{
        //    entry = entry.Replace(".", "");
        //    entry = entry.Replace("-", "");
        //}
        //do
        //{
        //    n = 0;
        //    BigInteger i = 1;
        //    BigInteger j = 1;
        //    for (i = 1, j = 1; i <= entry.Length; i++, j *= 10)
        //    {
        //        n += (BigInteger.Parse(entry) / j) % 10;
        //        n = (BigInteger)n;
        //    }
        //    entry = n.ToString();
        //}
        //while (n > 9);
        //Console.WriteLine(n);
    }
}
